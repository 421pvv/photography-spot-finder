<li data-id="{{_id}}" id="{{_id}}">
    <div class="submitted-item">
        <div class="submitted-content">
            <img alt="{{firstName}} {{lastName}}'s contest submission image" src="{{url}}">
        </div>
        <div class="submitted-meta ">
            <p>Posted by {{firstName}} {{lastName}}</p>
            <p>Posted at {{createdAt}}</p>
        </div>
        {{#if user}}
        <div class="vote-bar" data-id="{{_id}}">
            <h4>Cast your Vote!</h4>
            <i class="fa fa-thumbs-up"></i>
            <span class="user-vote">-</span>
            <i class="fa fa-thumbs-down"></i>
            <i class="fa fa-trash"></i>
            <i data-id="{{_id}}" class="submissionFlagButton fa fa-flag {{flag}}"></i>
        </div>
        {{/if}}
    </div>

</li>